package template;

import java.sql.Connection;

final class main {
    public static void main(String[] args) {

        DataSaver saver = new DataSaver();
        Data data = new Data();
        Connection connection = null;
//        saver.store(connection, data);
    }
}
